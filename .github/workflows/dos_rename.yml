
name: Rename dos file only

on:
  workflow_dispatch: # Manual trigger
  schedule:
    - cron: "0 0 * * *" # Daily at midnight UTC

permissions:
  contents: write # ✅ Allow pushing changes

jobs:
  rename-dos:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.github_pat_11B2UB54Y0tFL6y0tgwLMV_wGiZvndWSj58BoY8WikARiK1gLvEFsMvTIlhUGrZJmDUAOXWA4SGrYen37 }}

      - name: Configure Git
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

      - name: Rename old dos file
        run: |
          if [ -f dos ]; then
            TIMESTAMP=$(date +"%Y%m%d%H%M%S")
            mv dos "dos.${TIMESTAMP}"
            echo "Renamed dos to dos.${TIMESTAMP}"
          else
            echo "No existing dos file found."
          fi

      - name: Commit and push changes
        run: |
          git add .
          git commit -m "Renamed dos to dos.<timestamp>"
          git push origin main
